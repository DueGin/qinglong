services:
  web:
    image: duegin/qinglong:support-multi-device-login-v2 # 基于 Debian 的版本：whyour/qinglong:debian  
    volumes:
      - ./data:/ql/data
    ports:
      - "5700:5700"
    environment:
      QlBaseUrl: '/' # 部署路径非必须，以斜杠开头和结尾，比如 /test/
    restart: unless-stopped
